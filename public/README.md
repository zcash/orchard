# orchard (public API crate)

This directory contains the public API crate for Orchard. It re-exports the root package of this workspace (`orchard_internal`) as a thin facade, providing the stable public interface for downstream consumers.
